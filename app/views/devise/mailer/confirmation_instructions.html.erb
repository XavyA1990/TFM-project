<p><%= t('devise_views.mailer.confirmation_instructions.greeting', email: @email) %></p>

<p><%= t('devise_views.mailer.confirmation_instructions.message') %></p>

<p><%= link_to t('devise_views.mailer.confirmation_instructions.action'), confirmation_url(@resource, confirmation_token: @token) %></p>
