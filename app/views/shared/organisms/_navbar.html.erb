<nav
  data-controller="disclosure"
  class="relative z-30 bg-gray-800 dark:bg-gray-800 dark:after:pointer-events-none dark:after:absolute dark:after:inset-x-0 dark:after:bottom-0 dark:after:h-px dark:after:bg-white/10"
>
  <%
    nav_links =
      if current_tenant_for_ability.present?
        [
          {
            text: t("navbar.courses"),
            path: tenant_courses_path(tenant_slug: current_tenant_for_ability.slug),
            active: controller_path == "courses"
          }
        ]
      else
        []
      end
  %>
  <div class="mx-auto max-w-7xl px-2 sm:px-6 lg:px-8">
    <div class="relative flex h-16 items-center justify-between">
      <div class="absolute inset-y-0 left-0 flex items-center sm:hidden">
        <button
          type="button"
          aria-controls="mobile-menu"
          aria-expanded="false"
          data-action="click->disclosure#toggle"
          data-disclosure-target="button"
          class="relative inline-flex items-center justify-center rounded-md p-2 text-gray-400 hover:bg-white/5 hover:text-white focus:outline-2 focus:-outline-offset-1 focus:outline-indigo-500 hover:cursor-pointer"
        >
          <span class="absolute -inset-0.5"></span>
          <span class="sr-only">Open main menu</span>
          <svg
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="1.5"
            data-slot="icon"
            aria-hidden="true"
            data-disclosure-target="openIcon"
            class="size-6"
          >
            <path d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5" stroke-linecap="round" stroke-linejoin="round" />
          </svg>
          <svg
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="1.5"
            data-slot="icon"
            aria-hidden="true"
            data-disclosure-target="closeIcon"
            class="hidden size-6"
          >
            <path d="M6 18 18 6M6 6l12 12" stroke-linecap="round" stroke-linejoin="round" />
          </svg>
        </button>
      </div>
      <div class="flex flex-1 items-center justify-center sm:items-stretch sm:justify-start">
        <div class="flex shrink-0 items-center">
          <% if current_tenant_for_ability.present? %>
            <%= link_to tenant_root_path(tenant_slug: current_tenant_for_ability.slug), class: "flex items-center gap-x-3 rounded-lg px-2 py-1 text-white transition hover:bg-white/5" do %>
              <% if current_tenant_for_ability.logo_source.present? %>
                <%= image_tag current_tenant_for_ability.logo_source, alt: current_tenant_for_ability.name, class: "h-9 w-auto max-w-32 object-contain" %>
              <% else %>
                <span class="text-sm font-semibold"><%= current_tenant_for_ability.name %></span>
              <% end %>
            <% end %>
          <% end %>
        </div>
        <div class="hidden sm:ml-6 sm:block">
          <div class="flex space-x-4">
            <% nav_links.each do |link| %>
              <%= render "shared/molecules/nav_link", **link %>
            <% end %>
          </div>
        </div>
      </div>
      <div class="absolute inset-y-0 right-0 flex items-center pr-2 sm:static sm:inset-auto sm:ml-6 sm:pr-0">
        <% if !user_signed_in? && tenant_context_present? %>
        <div class="shrink-0">
            <%= render "shared/atoms/button_link", text: t("devise.sessions.new.sign_in"), path: new_user_session_path, variant: :secondary %>
          </div>
        <% end %>
        <% if user_signed_in? %>
          <%= render "shared/molecules/user_menu", dashboard_nav_links: dashboard_nav_links %>
        <% end %>
      </div>
    </div>
  </div>

  <div
    id="mobile-menu"
    data-disclosure-target="panel"
    class="hidden sm:hidden"
  >
    <div class="space-y-1 px-2 pt-2 pb-3">
      <% nav_links.each do |link| %>
        <%= render "shared/molecules/nav_link", **link, mobile: true %>
      <% end %>
    </div>
  </div>
</nav>
