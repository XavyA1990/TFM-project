<div class="hidden overflow-hidden sm:block">
  <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
    <table class="w-full text-left">
      <thead class="app-surface app-surface--admin-table-head">
        <tr>
          <% headers.each do |header| %>
            <th scope="col" class="app-text app-text--admin-strong px-3 py-3.5 text-left text-sm font-semibold"><%= header %></th>
          <% end %>
          <% action_column_count.times do %>
            <th scope="col" class="py-3.5 pl-3">
              <span class="sr-only"><%= t("actions.actions") %></span>
            </th>
          <% end %>
        </tr>
      </thead>
      <tbody>
        <% records.each do |record| %>
          <% actions = actions_builder.call(record) %>
          <tr>
            <% columns.each do |column| %>
              <td class="app-text app-text--admin-subtle px-3 py-4 text-sm"><%= value_builder.call(record, column) %></td>
            <% end %>
            <% actions.each do |action| %>
              <td class="py-4 pl-3 text-right text-sm font-medium">
                <%= render "shared/atoms/text_link",
                  text: action[:text],
                  path: action[:path],
                  class_name: action[:class_name] || "font-medium",
                  data: action[:data] %>
                <span class="sr-only">, <%= record_name_builder.call(record) %></span>
              </td>
            <% end %>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
