<%
  class_name = local_assigns[:class_name]
  data = local_assigns[:data]
  base_class = local_assigns.fetch(:base_class, "app-link app-link--primary")
%>

<%= link_to text, path, class: [base_class, class_name].compact.join(" "), data: data %>
